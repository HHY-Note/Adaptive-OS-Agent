[env]
# scx_utils is built before scx_adaptive's build.rs, so select a compatible
# BPF compiler for the whole scheduler build. Explicit caller values win.
BPF_CLANG = { value = "tools/bpf-clang", relative = true, force = false }
